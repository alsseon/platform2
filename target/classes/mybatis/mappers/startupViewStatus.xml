<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.startupViewStatus">

	<select id = "allQuotationList" resultType="startupViewStatusVO" parameterType="String">
		<![CDATA[
			select * from (select s.compname, m.MANUNAME,q.REQQUOTE, q.QUOTESTATUS, q.compid, q.MANUID
			from quotation q, manufacturer m, startup s 
			where q.manuid = m.id and q.compid = s.id) 
			where compid= #{compid}
		]]>
	</select>
	
</mapper>